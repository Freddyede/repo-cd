name: Cd-build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # Testing application
  ci:
    uses: ./tests.yml

  # Building application
  build:
    uses: ./build.yml
  # Deploy application
  deploy:
    needs: [ ci, build ]
    uses: ./deploy.yml